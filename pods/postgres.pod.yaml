apiVersion: v1
kind: Pod
metadata:
  name: postgres-db-pod
  labels:
    app: voting-app
    tier: backend
    type: postgres-db

spec:
  containers:
    - name: postgres-container
      image: postgres:alpine
      ports:
        - containerPort: 5432
